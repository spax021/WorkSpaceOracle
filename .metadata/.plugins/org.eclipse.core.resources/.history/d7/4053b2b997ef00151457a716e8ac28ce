package l8Interface;

/*An interface can extend other interfaces, 
 * just as a class subclass or extend another class. 
 * However, whereas a class can extend only one other class, 
 * an interface can extend any number of interfaces. The interface 
 * declaration includes a comma-separated list of all the interfaces that it extends.*/

interface OperateCar { //now its only visible in his own package, if its PUBLIC, everybody can see it
	
	int turn(Direction direction, double radius, double startSpeed, double endSpeed);

	int changeLanes(Direction direction, double startSpeed, double endSpeed);

	int signalTurn(Direction direction, boolean signalOn);

	int getRadarFront(double distanceToCar, double speedOfCar);

	int getRadarRear(double distanceToCar, double speedOfCar);
}
